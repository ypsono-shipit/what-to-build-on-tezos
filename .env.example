# Etherlink contract address (after deployment)
VITE_CONTRACT_ADDRESS=0x...

# Network: 'mainnet' or 'shadownet'
VITE_NETWORK=mainnet

# Private key for deployment only — never commit this
PRIVATE_KEY=0x...
